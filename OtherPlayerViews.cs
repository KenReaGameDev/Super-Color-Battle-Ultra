using UnityEngine;
using System.Collections;

public class OtherPlayerViews : MonoBehaviour {

	float screenWidth = Screen.width;
	public UISprite spriteMid;
	
	// Use this for initialization
	void Start () {	
		//screenWidth = Screen.width;	
		//int viewWidths = (int) (screenWidth / 3);
		
		//spriteMid.width = viewWidths - 6;
		//spriteMid.topAnchor.absolute = 100;
		//spriteMid.bottomAnchor.absolute = 0;
		
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
